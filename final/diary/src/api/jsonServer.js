import axios from 'axios'
// import {BASE_URL_LINK} from '@env'

export default axios.create({
    //this base URL will change each time you run npm run tunnel,
    //be sure to updated it here!!!
    baseURL:
  })